# macOS system files
.DS_Store
.DS_Store?

# Shopify theme files that should not be version-controlled
# (The theme.toml file stores store-specific configuration)
shopify.theme.toml

# Cache and temporary files
.theme-check-cache

# Node.js dependencies (if using Node for build processes)
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log

# Environment variables (if you're using any local .env files)
.env
.env.local

# Prettier config (optional, only if it's different for each dev)
.prettierrc.json

# Logs and debug files
*.log

# Vim and other editor swap files
*.swp

# GitHub Actions workflows
.github/workflows/node_modules/

.shopify